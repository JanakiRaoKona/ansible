# @format

- name: variables
  hosts: localhost
  vars: #variables declaration
    candidate: Rohan
    age: 27
    role: DevOps Engineer

  tasks:
    - name: print information1
      vars: #variables declaration
        candidate: Jani
      ansible.builtin.debug:
        msg: "Hi I am {{candidate}} age is: {{age}} role: {{role}}"

    - name: print information2
      ansible.builtin.debug:
        msg: "Hi I am {{candidate}} age is: {{age}} role: {{role}}"
